export enum ListControls {
  name = 'name',
}

export enum CardControls {
  name = 'name',
  priority = 'priority',
  checklist = 'checklist',
  member = 'member',
  list = 'list',
  dueDate = 'dueDate',
  description = 'description'
}
