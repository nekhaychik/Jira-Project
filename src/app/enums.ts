export enum ButtonAppearance {
  Primary = 'primary',
  Secondary = 'secondary'
}

export enum Icon {
  IconPlus = 'tuiIconPlus'
}

export enum Shape {
  Rounded = 'rounded',
  Square = 'square',
  Default = 'none'
}

export enum Status {
  Critical = 'critical',
  Blocked = 'blocked',
  Normal = 'normal'
}
